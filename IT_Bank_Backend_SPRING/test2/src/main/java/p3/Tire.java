package p3;

import org.springframework.stereotype.Component;

@Component
public class Tire {
	// 참조하는 대상이 없다 null
	// 비었다는 뜻이 아님.
	// 아무것도 없을 때 기본생성자가 존재 
	// 기본생성자로 객체를 생성하면 더이상 null이아니다.
	// 내용이 없는 것은 null 아니다.
	// 참조의 대상이 없는 것이 null이다.
}
